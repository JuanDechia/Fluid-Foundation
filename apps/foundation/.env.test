# This file contains non-secret environment variables for the test suite.
# It should be committed to version control.

DATABASE_URL="postgresql://testuser:testpassword@localhost:5433/saas_foundation_test"

# A predictable webhook secret for signing and verifying test payloads.
CLERK_WEBHOOK_SECRET="whsec_test_secret_for_real_signing"
